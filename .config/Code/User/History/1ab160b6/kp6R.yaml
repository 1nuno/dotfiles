version: "3.8"

services:
  tjsc:
    build: .
    image: tjsc:1.0
    container_name: tjsc
    restart: on-failure
    volumes:
      - ~/root/data:/usr/src/scraper/data
    stdin_open: true
    tty: true