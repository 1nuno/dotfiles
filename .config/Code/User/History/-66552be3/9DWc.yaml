version: "3.8"

services:
  sub:
    build: .
    image: tjsc:1.0
    container_name: tjsc
    restart: on-failure
    networks:
      - global-network

networks:
  global-network:
    driver: bridge