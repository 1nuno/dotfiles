version: "3.8"

services:
  tjsc:
    build: .
    image: tjsc:1.0
    container_name: tjsc
    restart: on-failure
    volumes:
      - ./data:/usr/src/scraper
    stdin_open: true
    tty: true 